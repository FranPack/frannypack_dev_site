export type Frontmatter = {
    title: string;
    date: string
    description: string
    tags: string[]
    thumbnailUrl: string
}

export type ProjectData = {
    title: string
    date: string
    description: string
    tags: string[]
    slug: string
    thumbnailUrl: string
}